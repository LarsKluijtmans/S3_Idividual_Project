stages:
- build
- test

build:
    stage: build
    script:
        - cd Individual_Project
        - ./gradlew build -x test
        - ./gradlew.bat --stop

test:
    stage: test
    script:
        - ./gradlew test
        - ./gradlew.bat --stop
