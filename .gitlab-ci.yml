stages:
- build
- test

build:
    stage: build
    script:
        - ./cd Individual_Project
        - ./gradlew build -x test
        - ./gradlew.bat --stop

test:
    stage: test
    script:
        - ./cd Individual_Project
        - ./gradlew test
        - ./gradlew.bat --stop
